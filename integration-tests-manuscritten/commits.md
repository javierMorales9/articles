# Commit range

This series is based on the current `ref/` snapshot.

The exact commit range is not the main source of truth for this series because the articles focus on the integration test setup and testing patterns rather than on a specific implementation diff.

Main files and directories to inspect:

- `apps/web/src/tests/integration/server/`
- `apps/web/jest.config.ts`
- `apps/web/jest.server.setup.ts`
